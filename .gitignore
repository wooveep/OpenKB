__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
venv/
*.db
.DS_Store
.env
.semantic-eval/

# Knowledge base test artifacts
raw/
wiki/
.openkb/
output/
kbs/

# Local only
docs/internal/
.claude/

# Heavy test-input documents for the examples (the old blanket `docs/` rule
# used to catch this dir at any depth; the anchored `docs/internal/` above
# does not). The PDFs already tracked on main stay tracked — this only stops
# new drops from being swept into commits.
examples/docs/

# Built Desktop Workbench bundle (regenerate with `cd frontend && npm install && npm run build`).
/openkb/desktop-web/

# Tauri build intermediates are platform-specific and regenerated by Cargo.
/desktop/src-tauri/target/
/desktop/src-tauri/gen/
/desktop/src-tauri/runtime/
/desktop/.build/
/desktop/release/

.graphify*
graphify-out/
openkb/graphify-out/
frontend/graphify-out/
.gitnexus/
.superpowers/
.codegraph/
.codex/
