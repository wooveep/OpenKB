# Remove legacy entry points after desktop acceptance

The legacy CLI, Web Workbench, and their dedicated REST/SSE interfaces are
removed after the Desktop Workbench passes acceptance for import, recovery,
question answering, document reading, settings, and Knowledge Page editing.
Legacy Knowledge Base migration is deferred to a later plan, so legacy data has
no supported application entry point between removal and that future migration.
